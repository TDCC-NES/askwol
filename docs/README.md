# docs/

Place a screenshot of the running web UI here as `screenshot.png` - it is
embedded at the top of the project `README.md`.

Suggested capture:

1. Start the app (`docker compose up` or `uvicorn askwol.web:app --reload`).
2. Validate a small, well-known ontology (e.g. FOAF) so the report shows
   namespaces, terms, and the class diagram.
3. Take a screenshot of roughly 1440×900 px and save it as `docs/screenshot.png`.
